# Compatibility-Core Cheat Sheet

```text
SKIPTIS
FRGETABTIT
FINALANSER(value)
PICKARANDOM(FROM(value)TO(value)W/O(value))
HAV A LIST "name" LIKE [items]:
TAKITOUT{name}[id]
```

Stable list IDs stay stable after removal.
